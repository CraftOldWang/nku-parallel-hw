#ifndef CONFIG_H
#define CONFIG_H

// #define NOT_USING_STRING_ARR // 定义宏，或者注释掉以禁用
// #define USING_ALIGNED

// 也许可以把所有设置放这里， 然后我们就只需要几个文件而不是一堆。
// #define USING_SIMD // 定义宏以启用SIMD MD5计算

#define THREAD_NUM 8 // 线程池的线程数

#endif